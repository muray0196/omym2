/**
 * Summary: Defines product copy shared across otherwise independent frontend surfaces.
 * Why: Prevents identical generic actions from drifting into feature-local duplicates.
 */
export const sharedCopy = {
  retry: "Try again",
} as const;
